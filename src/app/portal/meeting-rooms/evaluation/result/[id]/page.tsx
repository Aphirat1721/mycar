import {redirect} from "next/navigation";import {getCurrentUser} from "@/modules/identity/session";import {prisma} from "@/lib/prisma";import Link from "next/link";
export default async function Page({params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u)redirect('/login');const {id}=await params;const b=await prisma.meetingBooking.findUnique({where:{publicId:id},include:{room:{select:{nameTh:true}},evaluation:true}});if(!b||b.requesterId!==u.id||!b.evaluation)redirect('/portal/meeting-rooms/my-bookings');const e=b.evaluation;const items=[['ความพร้อมของห้องและอุปกรณ์',e.equipmentReadiness],['ความสะอาดเรียบร้อย',e.cleanliness],['การอำนวยความสะดวก / การให้บริการ',e.facilitationConvenience],['ความเหมาะสมของห้องกับการใช้งาน',e.roomSuitability],['ความพึงพอใจโดยรวม',e.overallRating]];return <section className="mx-auto max-w-3xl"><div className="mb-6"><p className="text-sm font-bold tracking-[0.14em] text-teal-700">EVALUATION RESULT</p><h1 className="mt-1 text-3xl font-black text-slate-900">ผลการประเมินความพึงพอใจ</h1><p className="mt-2 text-sm text-slate-500">{b.room.nameTh} · {b.title}</p></div><div className="space-y-4">{items.map(([label,value])=><div key={String(label)} className="flex items-center justify-between rounded-2xl border bg-white p-5"><span className="font-bold text-slate-700">{label}</span><span className="text-xl font-black text-amber-600">{value} / 5</span></div>)}<div className="rounded-2xl border bg-white p-5"><div className="font-bold text-slate-700">ปัญหาที่พบ</div><p className="mt-2 text-slate-600">{e.issueDetail||'ไม่พบปัญหา'}</p><div className="mt-4 font-bold text-slate-700">ความคิดเห็น / ข้อเสนอแนะ</div><p className="mt-2 whitespace-pre-wrap text-slate-600">{e.comment||'—'}</p></div></div><div className="mt-6 flex justify-end"><Link href="/portal/meeting-rooms/my-bookings" className="rounded-xl border px-5 py-3 font-bold text-slate-600">กลับรายการจองของฉัน</Link></div></section>}