"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { FileSpreadsheet, Filter, Printer, RefreshCw, X } from "lucide-react";
import DatePicker from "@/components/date-picker";
import { apiPath } from "@/lib/api-path";

const labels: Record<string, string> = { PENDING: "เธฃเธญเธเธดเธเธฒเธฃเธ“เธฒ", APPROVED: "เธญเธเธธเธกเธฑเธ•เธด", REJECTED: "เนเธกเนเธญเธเธธเธกเธฑเธ•เธด", CANCELLED: "เธขเธเน€เธฅเธดเธ", COMPLETED: "เน€เธชเธฃเนเธเธชเธดเนเธ" };
const input = "mt-1 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none transition focus:border-teal-500 focus:ring-4 focus:ring-teal-100";
type Master={publicId:string;nameTh?:string;licensePlate?:string|null;brand?:string|null;model?:string|null;firstName?:string;lastName?:string};
type StatRow={key:string;label:string;count:number};
type MonthRow={month:string;count:number};
type StatisticsData={total:number;byStatus:StatRow[];byMonth:MonthRow[]};
export default function StatisticsReportPage(){const[from,setFrom]=useState("");const[to,setTo]=useState("");const[status,setStatus]=useState("");const[departmentId,setDepartmentId]=useState("");const[vehicleId,setVehicleId]=useState("");const[driverId,setDriverId]=useState("");const[departments,setDepartments]=useState<Master[]>([]);const[vehicles,setVehicles]=useState<Master[]>([]);const[drivers,setDrivers]=useState<Master[]>([]);const[data,setData]=useState<StatisticsData>({total:0,byStatus:[],byMonth:[]});const[loading,setLoading]=useState(true);const[error,setError]=useState("");const params=useMemo(()=>{const p=new URLSearchParams();Object.entries({from,to,status,departmentId,vehicleId,driverId}).forEach(([k,v])=>{if(v)p.set(k,v)});return p},[from,to,status,departmentId,vehicleId,driverId]);const load=useCallback(async()=>{setLoading(true);setError("");try{const res=await fetch(apiPath(`/api/v1/reports/statistics?${params.toString()}`),{cache:"no-store"});const json=await res.json();if(!res.ok)throw new Error(json.error||"เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เนเธซเธฅเธ”เธฃเธฒเธขเธเธฒเธเนเธ”เน");setData(json)}catch(e){setError(e instanceof Error?e.message:"เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เนเธซเธฅเธ”เธฃเธฒเธขเธเธฒเธเนเธ”เน")}finally{setLoading(false)}},[params]);useEffect(()=>{fetch(apiPath("/api/v1/reports"),{cache:"no-store"}).then(r=>r.json()).then(d=>{setDepartments(d.departments||[]);setVehicles(d.vehicles||[]);setDrivers(d.drivers||[])}).catch(()=>undefined)},[]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer)},[load]);const print=()=>window.print();const exportCsv=()=>{const lines=["เธชเธ–เธฒเธเธฐ,เธเธณเธเธงเธ",...(data.byStatus||[]).map(r=>`${r.label},${r.count}`),"","เน€เธ”เธทเธญเธ,เธเธณเธเธงเธ",...(data.byMonth||[]).map(r=>`${r.month},${r.count}`)];const blob=new Blob(["\ufeff"+lines.join("\n")],{type:"text/csv;charset=utf-8"});const u=URL.createObjectURL(blob);const a=document.createElement("a");a.href=u;a.download="เธฃเธฒเธขเธเธฒเธเธชเธ–เธดเธ•เธดเธเธณเธเธญเนเธเนเธฃเธ–.csv";a.click();URL.revokeObjectURL(u)};return <div className="space-y-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-sm font-semibold text-teal-700">เธฃเธฒเธขเธเธฒเธ</p><h1 className="mt-1 text-3xl font-black tracking-tight text-slate-900">เธฃเธฒเธขเธเธฒเธเธชเธ–เธดเธ•เธดเธเธณเธเธญเนเธเนเธฃเธ–</h1><p className="mt-2 text-sm text-slate-500">เธชเธฃเธธเธเธเธณเธเธงเธเธเธณเธเธญเนเธขเธเธ•เธฒเธกเธชเธ–เธฒเธเธฐเนเธฅเธฐเนเธเธงเนเธเนเธกเธฃเธฒเธขเน€เธ”เธทเธญเธ</p></div><div className="flex gap-2 print:hidden"><button onClick={exportCsv} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-sm font-bold"><FileSpreadsheet size={17}/>เธชเนเธเธญเธญเธ CSV</button><button onClick={print} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-sm font-bold"><Printer size={17}/>เธเธดเธกเธเน / PDF</button></div></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm print:hidden"><div className="mb-4 flex items-center gap-2"><Filter size={17} className="text-teal-700"/><h2 className="font-black">เธ•เธฑเธงเธเธฃเธญเธเธฃเธฒเธขเธเธฒเธ</h2></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-6"><label className="text-xs font-bold text-slate-600">เธ•เธฑเนเธเนเธ•เนเธงเธฑเธเธ—เธตเน<DatePicker value={from} onChange={setFrom}/></label><label className="text-xs font-bold text-slate-600">เธ–เธถเธเธงเธฑเธเธ—เธตเน<DatePicker value={to} onChange={setTo} min={from||undefined}/></label><label className="text-xs font-bold text-slate-600">เธชเธ–เธฒเธเธฐ<select value={status} onChange={e=>setStatus(e.target.value)} className={input}><option value="">เธ—เธฑเนเธเธซเธกเธ”</option>{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="text-xs font-bold text-slate-600">เธซเธเนเธงเธขเธเธฒเธ<select value={departmentId} onChange={e=>setDepartmentId(e.target.value)} className={input}><option value="">เธ—เธฑเนเธเธซเธกเธ”</option>{departments.map(d=><option key={d.publicId} value={d.publicId}>{d.nameTh}</option>)}</select></label><label className="text-xs font-bold text-slate-600">เธฃเธ–เธขเธเธ•เน<select value={vehicleId} onChange={e=>setVehicleId(e.target.value)} className={input}><option value="">เธ—เธฑเนเธเธซเธกเธ”</option>{vehicles.map(v=><option key={v.publicId} value={v.publicId}>{[v.licensePlate,v.brand,v.model].filter(Boolean).join(" โ€ข ")||"เนเธกเนเธฃเธฐเธเธธ"}</option>)}</select></label><label className="text-xs font-bold text-slate-600">เธเธเธฑเธเธเธฒเธเธเธฑเธเธฃเธ–<select value={driverId} onChange={e=>setDriverId(e.target.value)} className={input}><option value="">เธ—เธฑเนเธเธซเธกเธ”</option>{drivers.map(d=><option key={d.publicId} value={d.publicId}>{d.firstName} {d.lastName}</option>)}</select></label></div><div className="mt-4 flex justify-end gap-2"><button onClick={()=>{setFrom("");setTo("");setStatus("");setDepartmentId("");setVehicleId("");setDriverId("")}} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 px-4 text-sm font-bold"><X size={16}/>เธฅเนเธฒเธเธ•เธฑเธงเธเธฃเธญเธ</button><button onClick={load} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-teal-600 px-5 text-sm font-bold text-white"><RefreshCw size={17}/>เธเนเธเธซเธฒ</button></div></section>{error?<div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm font-semibold text-rose-700">{error}</div>:null}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-6"><div className="rounded-2xl border border-slate-200 bg-white p-5"><div className="text-xs font-bold text-slate-500">เธเธณเธเธญเธ—เธฑเนเธเธซเธกเธ”</div><div className="mt-1 text-3xl font-black text-slate-900">{loading?"โ€”":data.total}</div></div>{(data.byStatus||[]).map(s=><div key={s.key} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="text-xs font-bold text-slate-500">{s.label}</div><div className="mt-1 text-3xl font-black text-slate-900">{s.count}</div></div>)}</div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4"><h2 className="font-black">เนเธเธงเนเธเนเธกเธเธณเธเธงเธเธเธณเธเธญเธฃเธฒเธขเน€เธ”เธทเธญเธ</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[500px] text-sm"><thead className="bg-slate-50 text-xs font-bold text-slate-600"><tr><th className="px-4 py-3 text-left">เน€เธ”เธทเธญเธ</th><th className="px-4 py-3 text-right">เธเธณเธเธงเธเธเธณเธเธญ</th></tr></thead><tbody className="divide-y divide-slate-100">{(data.byMonth||[]).map(r=><tr key={r.month}><td className="px-4 py-3">{r.month}</td><td className="px-4 py-3 text-right font-bold">{r.count}</td></tr>)}{!loading&&!data.byMonth?.length?<tr><td colSpan={2} className="px-4 py-10 text-center text-slate-500">เนเธกเนเธเธเธเนเธญเธกเธนเธฅเธ•เธฒเธกเน€เธเธทเนเธญเธเนเธ</td></tr>:null}</tbody></table></div></section></div>}



