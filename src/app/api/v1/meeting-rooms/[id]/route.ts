import type { Prisma } from "@/generated/prisma";
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireAdmin } from "@/lib/authorization";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin("MEETING_ROOMS");const id=(await params).id;const b=await request.json();const data:Prisma.MeetingRoomUpdateInput={};if(b.nameTh!==undefined)data.nameTh=String(b.nameTh).trim();if(b.location!==undefined)data.location=b.location?String(b.location):null;if(b.capacity!==undefined)data.capacity=Number(b.capacity);if(b.description!==undefined)data.description=b.description?String(b.description):null;if(b.imagePath!==undefined)data.imagePath=b.imagePath?String(b.imagePath):null;if(b.status!==undefined)data.status=b.status;const row=await prisma.meetingRoom.update({where:{publicId:id},data});return NextResponse.json({data:row});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เนเธเนเนเธเนเธ”เน"},{status:400})}}
